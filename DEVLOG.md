# DEVLOG (작업 일지)

이 문서는 '모두의 맛집' 프로젝트의 일일 작업 내역, 주요 의사결정 및 이슈 해결 과정을 기록하는 공간입니다.
AI 어시스턴트는 새로운 세션을 시작할 때 이 파일을 먼저 참조하여 문맥을 파악합니다.

---

## 2026-05-17 (일)

### 🚀 Done (완료된 작업)
- **Supabase 데이터베이스 연동 및 마이그레이션**:
  - `restaurant_submissions` 테이블 생성 (유저의 제보 데이터를 임시 저장하는 버퍼 테이블).
  - 관리자 및 익명(anon) 유저가 안전하게 데이터를 삽입할 수 있도록 RLS(Row Level Security) INSERT 정책 적용 완료.
- **AI 팩트체크 파이프라인 고도화**:
  - **이슈**: 기존 oEmbed API가 일부 유튜브 영상(쇼츠 등)에서 `401 Unauthorized`를 반환하여 메타데이터가 누락되는 현상 발견.
  - **해결**: oEmbed를 걷어내고 공식 `YouTube Data API v3`를 연동하도록 수정. 정규식을 통해 모든 형태의 유튜브 URL에서 `videoId`를 추출하고, 영상 제목과 채널명뿐만 아니라 영상 설명(Description)까지 Gemini 모델에 넘기도록 프롬프트 강화.
- **제보 UI/UX 비동기 -> 동기 전환**:
  - 제보 직후 "기다려달라"는 알림 대신, 즉시 백엔드에서 AI 심사를 대기하고 그 결과(승인/반려 및 AI 판단 사유)를 화면에 실시간으로 표시하도록 로직 개선 (Instant Feedback).
  - 승인 후 모달을 닫았을 때 `refresh-restaurants` 커스텀 이벤트를 발생시켜 지도를 새로고침 없이 즉시 동기화하도록 조치.
- **앱 아이콘(파비콘) 디자인 및 최적화**:
  - `map_marker`와 `play_button`을 결합한 "The Delicious Play" 컨셉 확정.
  - 식욕을 자극하는 '웜 탠저린(오렌지)' 컬러 채택.
  - AI 생성 이미지 가장자리의 하얀 안티앨리어싱 찌꺼기를 Python 이미지 스크립트(Bounding box crop + Lanczos Resize + Squircle Mask)를 통해 100% 무결점 투명화 처리.
  - Next.js 13+ App Router 규격에 맞게 `src/app/icon.png` 및 `src/app/apple-icon.png` 로컬 적용.

### 🧱 Blocker & Resolutions (문제 및 해결 방안)
- **Next.js 파비콘 캐싱 문제**: Next.js 로컬 개발 서버와 브라우저의 강력한 캐싱으로 인해 파비콘이 즉각 업데이트되지 않음. `.next` 폴더를 강제 삭제하고 서버를 재시작한 뒤 브라우저 하드 리프레시(Ctrl+Shift+R)를 수행하여 해결.

### 🔜 Next To-do (다음 할 일)
- **사용자 최종 육안 검수 및 배포**:
  - 로컬 개발 서버(`npm run dev`) 구동 후 브라우저상에서 서울역 맛집 10곳의 실제 크리에이터 프로필 및 유튜브 영상이 엑박 없이 정상 출력되는지 검수.
  - 카카오 맵 조작 편의성(드래그 및 축소/확대 튕김 해결 여부) 최종 검수.
  - 검수 통과 시 AIT(App in Toss) 플랫폼 배포 진행.

---

## 2026-05-24 (일)
### 🚀 Done (완료된 작업)
- **공유형 테마 지도 및 실시간 모바일 투표 시스템 구현**:
  - `MobileGroupVotingView.tsx` 전격 추가하여 모바일 전용 맛집 투표 바텀시트 UI 완성 (구글 스티치 디자인 토큰 탑재).
  - PC와 모바일 뷰를 유연하게 스위칭하도록 `GroupMapClient.tsx`에 `useIsMobile()` 기반 Fab 스위치 구현.
  - Supabase Realtime 기술을 접목하여 여러 기기 간 식당 추가 및 하트 투표 정보 즉시 무중단 동기화.
- **미식 게임 위젯(GameFAB) 통합**:
  - 메인 화면 우측 하단의 기존 주사위 버튼을 터치/호버 시 미식 밸런스 게임(이상형 월드컵) 및 랜덤 뽑기가 펼쳐지는 위젯으로 확장.
  - 밸런스 게임 우승 메뉴 키워드를 통합 검색과 즉시 바인딩하여 필터링하는 파이프라인 연계.
- **유튜브 프로필 및 영상 정보 수집 장애 조치**:
  - 서울역 10대 리얼 맛집 비디오 ID 전면 교정 및 원격 Supabase DB의 깨진 잔재 깔끔히 제거(`truncate_db.js`).
  - 구글 프로필 이미지 토큰 만료 엑박 현상을 영구 차단하는 3중 방어막(`onError` 폴백, `ui-avatars.com` 생성기, 카테고리별 썸네일 바인딩) 구축.
  - 카카오 맵의 `center` 속성에 유동적인 리렌더링 상태 대신 고정 상수를 바인딩하고 `panTo` 기반 이동으로 변경하여, 드래그 중 지도가 제자리로 튕기는 고질적 버그 완벽 조치.
- **Next.js 16 빌드 무결성 확보**:
  - `npm run build` 가동을 통해 단 하나의 경고나 에러 없이 정적 컴파일 및 TypeScript 타입 체크가 통과함을 전수 검증 완료.

### 🧱 Blocker & Resolutions (문제 및 해결 방안)
- **카카오맵 지도 조작 굳음/튕김 버그**: `react-kakao-maps-sdk`의 `Map` 컴포넌트 `center` 속성에 동적 반응형 상태인 위경도를 직접 매핑했을 때, 드래그 종료 시 부모 리렌더링이 발생하여 계속 원래의 서울 용산 중심 좌표로 튕겨서 되돌아오는 기현상 발생. 이를 해결하기 위해 `center`는 영구 상수(`INITIAL_CENTER`)로 완전히 고정하고 카메라의 부드러운 위치 스크롤은 카카오 맵 API의 `map.panTo()` 메서드로 제어하도록 수정하여 드래그 잠금 버그 완벽 예방.

### 🔜 Next To-do (다음 할 일)
- **실서비스 적용 및 지속 모니터링**:
  - 유튜버의 채널 이니셜 아바타 등 3중 백업 이미지 로직이 실제 오프라인 또는 CDN 장애 상황에서도 실시간으로 잘 방어되는지 주기적 점검.
